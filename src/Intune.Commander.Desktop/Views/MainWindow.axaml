<suki:SukiWindow xmlns="https://github.com/avaloniaui"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:vm="using:Intune.Commander.Desktop.ViewModels"
                 xmlns:views="using:Intune.Commander.Desktop.Views"
                 xmlns:conv="using:Intune.Commander.Desktop.Converters"
                 xmlns:models="using:Intune.Commander.Desktop.Models"
                 xmlns:suki="https://github.com/kikipoulet/SukiUI"
                 xmlns:mi="clr-namespace:Material.Icons.Avalonia;assembly=Material.Icons.Avalonia"
                 xmlns:icons="clr-namespace:Material.Icons;assembly=Material.Icons"
                 xmlns:controls="using:Intune.Commander.Desktop.Views.Controls"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 mc:Ignorable="d" d:DesignWidth="1100" d:DesignHeight="1050"
                 x:Class="Intune.Commander.Desktop.Views.MainWindow"
                 x:Name="MainWin"
                 x:DataType="vm:MainWindowViewModel"
                 Title="{Binding WindowTitle}"
                 FontSize="{StaticResource FontSizeBody}"
                 Width="1200" Height="800"
                 MinWidth="1024" MinHeight="768"
                 WindowStartupLocation="CenterScreen"
                 >

    <Design.DataContext>
        <vm:MainWindowViewModel />
    </Design.DataContext>

    <DockPanel>
        <!-- Menu Bar -->
        <Menu DockPanel.Dock="Top" IsVisible="{Binding IsConnected}">
            <MenuItem Header="File">
                <MenuItem Header="Refresh" Command="{Binding RefreshCommand}" InputGesture="F5" />
                <Separator />
                <MenuItem Header="Disconnect" Command="{Binding DisconnectCommand}" />
            </MenuItem>
            <MenuItem Header="Reports">
                <MenuItem Header="Assignment Report..." x:Name="MenuAssignmentReport" Click="OnAssignmentReportClick" IsEnabled="{Binding IsConnected}" />
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="Check for Updates..." x:Name="MenuCheckUpdates" Click="OnCheckForUpdatesClick" />
                <Separator />
                <MenuItem Header="Permissions..." x:Name="MenuPermissions" Click="OnPermissionsButtonClick" IsEnabled="{Binding IsConnected}">
                    <MenuItem.Icon>
                        <mi:MaterialIcon Kind="Key" />
                    </MenuItem.Icon>
                </MenuItem>
                <Separator />
                <MenuItem Header="About Intune Commander" x:Name="MenuAbout" Click="OnAboutClick" />
            </MenuItem>

        </Menu>

        <!-- Status Bar -->
        <Border DockPanel.Dock="Bottom" Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}"
                Padding="8,6" MinHeight="36" IsVisible="{Binding IsConnected}">
            <DockPanel>
                <!-- Right side: Debug Log link + progress bar -->
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Spacing="2"
                            VerticalAlignment="Center" HorizontalAlignment="Right">
                    <ProgressBar IsIndeterminate="True" IsVisible="{Binding IsBusy}"
                                 Width="200" Height="12"
                                 Foreground="{DynamicResource ProgressBarForegroundBrush}" Background="{DynamicResource ProgressBarBackgroundBrush}"
                                 VerticalAlignment="Center"
                                 CornerRadius="{StaticResource RadiusMedium}">
                    </ProgressBar>
                    <Border Background="Transparent" Cursor="Hand" Padding="4,2"
                            PointerPressed="OnDebugLogLinkPressed">
                        <TextBlock Text="Debug Log" FontSize="{StaticResource FontSizeBody}"
                                   Foreground="{DynamicResource SystemAccentColor}"
                                   TextDecorations="Underline"
                                   VerticalAlignment="Center" />
                    </Border>
                </StackPanel>

                <!-- Left side: Status text + error text -->
                <StackPanel Orientation="Vertical" Spacing="2" VerticalAlignment="Center">
                    <TextBlock Text="{Binding StatusText}" FontSize="{StaticResource FontSizeBody}" />
                    <TextBlock Text="{Binding CacheStatusText}" FontSize="{StaticResource FontSizeCaption}"
                               Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}" />
                    <SelectableTextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource AppErrorTextBrush}" FontSize="11"
                                         TextWrapping="Wrap"
                                         IsVisible="{Binding ErrorMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource AppErrorTextBrush}" FontSize="{StaticResource FontSizeCaption}"
                               TextWrapping="Wrap"
                               IsVisible="{Binding ErrorMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Main Content -->
        <Panel>
            <!-- Login View (shown when not connected) -->
            <ContentControl Content="{Binding LoginViewModel}"
                            IsVisible="{Binding !IsConnected}">
                <ContentControl.ContentTemplate>
                    <DataTemplate DataType="vm:LoginViewModel">
                        <views:LoginView />
                    </DataTemplate>
                </ContentControl.ContentTemplate>
            </ContentControl>

            <!-- Connected Content (shown when connected) -->
            <DockPanel IsVisible="{Binding IsConnected}">
                <!-- Toolbar -->
                <Border DockPanel.Dock="Top" Padding="12,10"
                        Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}">
                    <DockPanel>
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <DropDownButton Content="Actions" Classes="Small">                               
                                <DropDownButton.Flyout>
                                <MenuFlyout Placement="BottomEdgeAlignedLeft">
                                    <MenuItem Header="Export Selected" Command="{Binding ExportSelectedCommand}" />
                                    <MenuItem Header="Export All" Command="{Binding ExportAllCommand}" />
                                    <Separator />
                                    <MenuItem Header="Import..." x:Name="ImportMenuItem" />
                                </MenuFlyout>
                            </DropDownButton.Flyout>
                            </DropDownButton>

                            <Border Width="1" Background="{DynamicResource SystemControlForegroundBaseMediumLowBrush}" Margin="4,2" />
                            <Button Command="{Binding DownloadAllToCacheCommand}"
                                    IsVisible="{Binding !IsDownloadingAll}"
                                    ToolTip.Tip="Downloads all Intune data types into the local cache"
                                    FontWeight="SemiBold"
                                    Background="{DynamicResource SuccessBadgeBrush}"
                                    Foreground="White"
                                    Classes="Flat Accent Small"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Right">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <mi:MaterialIcon Kind="Download" />
                                    <TextBlock Text="Download All to Cache" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding CancelDownloadAllCommand}"
                                    IsVisible="{Binding IsDownloadingAll}"
                                    Classes="Flat Accent Small">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <mi:MaterialIcon Kind="Stop" />
                                    <TextBlock Text="Cancel Download" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                            <ComboBox ItemsSource="{Binding SwitcherProfiles}"
                                      SelectedItem="{Binding SelectedSwitchProfile}"
                                      MinWidth="180"
                                      VerticalAlignment="Center"
                                      IsEnabled="{Binding !IsBusy}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" />
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                            <Button Content="Disconnect" Classes="Small" Command="{Binding DisconnectCommand}" />
                        </StackPanel>
                    </DockPanel>
                </Border>

                <!-- Download All Progress -->
                <ProgressBar DockPanel.Dock="Top"
                             IsIndeterminate="False"
                             Maximum="100"
                             Value="{Binding DownloadProgressPercent}"
                             IsVisible="{Binding IsDownloadingAll}"
                             Height="4"
                             Foreground="{DynamicResource ProgressBarForegroundBrush}"
                             Background="{DynamicResource ProgressBarBackgroundBrush}"
                             CornerRadius="0" />
                <TextBlock DockPanel.Dock="Top"
                           Text="{Binding DownloadProgress}"
                           IsVisible="{Binding IsDownloadingAll}"
                           FontSize="{StaticResource FontSizeBody}"
                           HorizontalAlignment="Center"
                           Margin="0,2,0,0"
                           Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}" />

                <!-- Body: custom sidebar + content -->
                <Grid ColumnDefinitions="220,*">

                    <!-- Sidebar -->
                    <DockPanel Grid.Column="0"
                               Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}">

                        <!-- App title header -->
                        <Border DockPanel.Dock="Top" Padding="16,14,16,10"
                                BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"
                                BorderThickness="0,0,0,1">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <mi:MaterialIcon Kind="{x:Static icons:MaterialIconKind.ShieldKey}"
                                                 Width="20" Height="20"
                                                 Foreground="{DynamicResource SystemControlForegroundAccentBrush}" />
                                <TextBlock Text="Intune Commander" FontSize="{StaticResource FontSizeSubheading}" FontWeight="SemiBold"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                        </Border>

                        <!-- Scrollable nav list -->
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <StackPanel Spacing="0" Margin="0,8,0,16">

                                <!-- OVERVIEW section label -->
                                <TextBlock Text="OVERVIEW" FontSize="{StaticResource FontSizeCaption}" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AppTextSecondaryBrush}"
                                           Margin="18,4,0,2" />

                                <!-- Overview nav item -->
                                <Button Tag="Overview" Click="OnNavItemClick"
                                        Classes="NavItem"
                                        Classes.active="{Binding IsOverviewCategory}">
                                    <StackPanel Orientation="Horizontal" Spacing="10">
                                        <mi:MaterialIcon Kind="{x:Static icons:MaterialIconKind.ViewDashboard}"
                                                         Width="16" Height="16" VerticalAlignment="Center" />
                                        <TextBlock Text="Overview" FontSize="{StaticResource FontSizeBody}" VerticalAlignment="Center" />
                                    </StackPanel>
                                </Button>

                                <!-- Nav groups (each group becomes a section) -->
                                <ItemsControl ItemsSource="{Binding NavGroups}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="vm:NavCategoryGroup">
                                            <StackPanel Margin="0,10,0,0">
                                                <!-- Section header label -->
                                                <TextBlock Text="{Binding NameUpper}" FontSize="{StaticResource FontSizeCaption}" FontWeight="SemiBold"
                                                           Foreground="{DynamicResource AppTextSecondaryBrush}"
                                                           Margin="18,4,0,2" />
                                                <!-- Child nav items -->
                                                <ItemsControl ItemsSource="{Binding Children}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate x:DataType="vm:NavCategory">
                                                            <Button Tag="{Binding Name}" Click="OnNavItemClick"
                                                                    Classes="NavItem"
                                                                    Classes.active="{Binding IsSelected}">
                                                                <StackPanel Orientation="Horizontal" Spacing="10">
                                                                    <mi:MaterialIcon Kind="{Binding Icon}" Width="18" Height="18" VerticalAlignment="Center" />
                                                                    <TextBlock Text="{Binding Name}" FontSize="{StaticResource FontSizeBody}"
                                                                               VerticalAlignment="Center"
                                                                               TextTrimming="CharacterEllipsis" />
                                                                </StackPanel>
                                                            </Button>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                            </StackPanel>
                        </ScrollViewer>
                    </DockPanel>

                    <!-- Content area -->
                    <Grid Grid.Column="1" ColumnDefinitions="*,Auto">

                        <!-- Item List (center) -->
                        <DockPanel Margin="8"
                                   IsVisible="{Binding !IsOverviewCategory}">

                            <!-- Category header with column chooser -->
                            <DockPanel DockPanel.Dock="Top" Margin="0,0,0,8">
                                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Spacing="4">
                                    <Button Content="Export CSV"
                                            Command="{Binding ExportAppAssignmentsCsvCommand}"
                                            Classes="Small"
                                            VerticalAlignment="Center"
                                            IsVisible="{Binding IsAppAssignmentsCategory}" />
                                    <Button Content="Export CSV"
                                            Command="{Binding ExportGroupsCsvCommand}"
                                            Classes="Small"
                                            VerticalAlignment="Center">
                                        <Button.IsVisible>
                                            <MultiBinding Converter="{x:Static BoolConverters.Or}">
                                                <Binding Path="IsDynamicGroupsCategory" />
                                                <Binding Path="IsAssignedGroupsCategory" />
                                            </MultiBinding>
                                        </Button.IsVisible>
                                    </Button>
                                    <Button Content="Export CSV"
                                            Command="{Binding ExportSettingsCatalogCsvCommand}"
                                            Classes="Small"
                                            VerticalAlignment="Center"
                                            IsVisible="{Binding IsSettingsCatalogCategory}"
                                            ToolTip.Tip="Export Settings Catalog policies list to CSV" />
                                    <Button Content="Export CSV"
                                            Command="{Binding ExportCompliancePoliciesCsvCommand}"
                                            Classes="Small"
                                            VerticalAlignment="Center"
                                            IsVisible="{Binding IsCompliancePolicyCategory}"
                                            ToolTip.Tip="Export Compliance Policies list to CSV" />
                                    <Button Content="Export CSV"
                                            Command="{Binding ExportAdminTemplatesCsvCommand}"
                                            Classes="Small"
                                            VerticalAlignment="Center"
                                            IsVisible="{Binding IsAdministrativeTemplatesCategory}"
                                            ToolTip.Tip="Export Administrative Templates list to CSV" />
                                    <Button Command="{Binding ExportConditionalAccessPowerPointCommand}"
                                            Classes="Small"
                                            VerticalAlignment="Center"
                                            IsVisible="{Binding IsConditionalAccessCategory}"
                                            ToolTip.Tip="Export Conditional Access policies to PowerPoint presentation">
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <mi:MaterialIcon Kind="Presentation" />
                                            <TextBlock Text="Export PowerPoint" VerticalAlignment="Center" />
                                        </StackPanel>
                                    </Button>
                                    <Button Content="⋮ Columns"
                                            x:Name="ColumnChooserButton"
                                            Classes="Small"
                                            VerticalAlignment="Center" />
                                </StackPanel>
                                <TextBlock Text="{Binding SelectedCategory.Name, FallbackValue='Items'}"
                                           FontSize="{StaticResource FontSizeHeading}" FontWeight="SemiBold"
                                           VerticalAlignment="Center" />
                            </DockPanel>

                            <!-- Search / filter box -->
                            <TextBox DockPanel.Dock="Top"
                                     Text="{Binding SearchText, Mode=TwoWay}"
                                     Watermark="🔍 Search..."
                                     Margin="0,0,0,8"
                                     Padding="8,6"
                                     FontSize="{StaticResource FontSizeBody}" />

                            <!-- Single dynamic DataGrid — columns built in code-behind -->
                            <Panel>
                                <DataGrid x:Name="MainDataGrid"
                                          AutoGenerateColumns="False"
                                          IsReadOnly="True"
                                          CanUserResizeColumns="True"
                                          GridLinesVisibility="Horizontal"
                                          FontSize="{StaticResource FontSizeBody}"
                                          BorderThickness="1"
                                          BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}">
                                    <DataGrid.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Refresh Item from Graph"
                                                      Command="{Binding RefreshSelectedItemCommand}"
                                                      IsEnabled="{Binding CanRefreshSelectedItem}" />
                                            <Separator />
                                            <MenuItem Header="Export Selected"
                                                      Command="{Binding ExportSelectedCommand}" />
                                            <MenuItem Header="Copy Details to Clipboard"
                                                      Command="{Binding CopyDetailsToClipboardCommand}" />
                                        </ContextMenu>
                                    </DataGrid.ContextMenu>
                                </DataGrid>
                                <Border IsVisible="{Binding IsCurrentCategoryEmpty}"
                                        VerticalAlignment="Center" HorizontalAlignment="Center"
                                        Padding="32,20" CornerRadius="{StaticResource RadiusLarge}"
                                        Background="{DynamicResource SystemControlBackgroundAltHighBrush}">
                                    <StackPanel Spacing="10" HorizontalAlignment="Center">
                                        <mi:MaterialIcon Kind="InboxOutline"
                                                         Width="48"
                                                         Height="48"
                                                         Foreground="{DynamicResource AppTextSecondaryBrush}"
                                                         HorizontalAlignment="Center"
                                                         AutomationProperties.Name="Empty inbox icon"
                                                         ToolTip.Tip="Empty inbox" />
                                        <TextBlock Text="No items found"
                                                   FontSize="{StaticResource FontSizeHeading}" FontWeight="SemiBold"
                                                   HorizontalAlignment="Center" />
                                        <TextBlock Text="This category is empty or no items match your search."
                                                   FontSize="{StaticResource FontSizeBody}" Foreground="{DynamicResource AppTextSecondaryBrush}"
                                                   HorizontalAlignment="Center" TextWrapping="Wrap" MaxWidth="300" />
                                    </StackPanel>
                                </Border>
                            </Panel>
                        </DockPanel>

                        <!-- Overview Dashboard (spans center + detail columns) -->
                        <views:OverviewView Grid.ColumnSpan="2"
                                            DataContext="{Binding Overview}"
                                            IsVisible="{Binding $parent[suki:SukiWindow].((vm:MainWindowViewModel)DataContext).IsOverviewCategory}" />

                        <!-- Detail Pane (right) -->
                        <DockPanel Grid.Column="1" Margin="16,8,8,8"
                                   IsVisible="{Binding !IsOverviewCategory}">
                            <DockPanel DockPanel.Dock="Top" Margin="0,0,0,8">
                                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Spacing="4">
                                    <Button Content="🔄 Refresh"
                                            Command="{Binding RefreshSelectedItemCommand}"
                                            IsEnabled="{Binding CanRefreshSelectedItem}"
                                            Classes="Small"
                                            VerticalAlignment="Center"
                                            ToolTip.Tip="Refresh this item from Graph" />
                                    <Button Content="📄 JSON"
                                            Command="{Binding ViewRawJsonCommand}"
                                            IsEnabled="{Binding CanRefreshSelectedItem}"
                                            Classes="Small"
                                            VerticalAlignment="Center"
                                            ToolTip.Tip="View raw Graph JSON object" />
                                    <Button Content="📋 Copy"
                                            Command="{Binding CopyDetailsToClipboardCommand}"
                                            Classes="Small"
                                            VerticalAlignment="Center" />
                                </StackPanel>
                                <TextBlock Text="Details"
                                           FontSize="{StaticResource FontSizeHeading}" FontWeight="SemiBold"
                                           VerticalAlignment="Center"
                                           Padding="20"/>
                            </DockPanel>

                            <!-- Loading indicator for detail fetch -->
                            <ProgressBar DockPanel.Dock="Top" IsIndeterminate="True"
                                         IsVisible="{Binding IsLoadingDetails}"
                                         Height="2" Margin="0,0,0,4" />

                            <!-- No selection placeholder -->
                            <TextBlock Text="Select an item to view details"
                                       VerticalAlignment="Center" HorizontalAlignment="Center"
                                       Foreground="{DynamicResource AppTextSecondaryBrush}" FontSize="{StaticResource FontSizeSubheading}"
                                       x:Name="PlaceholderNoSelection">
                                <TextBlock.IsVisible>
                                    <MultiBinding Converter="{x:Static BoolConverters.And}">
                                        <Binding Path="SelectedConfiguration" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedCompliancePolicy" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedSettingsCatalogPolicy" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedApplication" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedEndpointSecurityIntent" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedAdministrativeTemplate" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedEnrollmentConfiguration" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedAppProtectionPolicy" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedManagedDeviceAppConfiguration" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedTargetedManagedAppConfiguration" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedTermsAndConditions" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedScopeTag" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedRoleDefinition" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedIntuneBrandingProfile" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedAzureBrandingLocalization" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedAutopilotProfile" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedDeviceHealthScript" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedMacCustomAttribute" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedFeatureUpdateProfile" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedNamedLocation" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedAuthenticationStrengthPolicy" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedAuthenticationContextClassReference" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedTermsOfUseAgreement" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedConditionalAccessPolicy" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedAssignmentFilter" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedPolicySet" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedAppAssignmentRow" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedDynamicGroupRow" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedAssignedGroupRow" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedDeviceManagementScript" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedDeviceShellScript" Converter="{x:Static ObjectConverters.IsNull}" />
                                        <Binding Path="SelectedComplianceScript" Converter="{x:Static ObjectConverters.IsNull}" />
                                    </MultiBinding>
                                </TextBlock.IsVisible>
                            </TextBlock>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Device Configuration details               -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedConfiguration, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">

                                    <!-- Section: Properties -->
                                    <TextBlock Text="Properties" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedConfiguration}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Platform / Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemTypeName}"
                                                                     TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Created" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding CreatedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Last Modified" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding LastModifiedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Version" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Version}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <StackPanel Spacing="2"
                                                        IsVisible="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemRoleScopeTags.Count}">
                                                <TextBlock Text="Role Scope Tags" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemRoleScopeTags, Converter={x:Static conv:StringListConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Configuration Details-->
                                    <TextBlock Text="Configuration Details" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="OMA-URI Settings" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding SelectedItemOmaSettingsCount}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Assignments -->
                                    <controls:AssignmentListControl Assignments="{Binding SelectedItemAssignments}" />

                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Compliance Policy details                  -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedCompliancePolicy, Converter={x:Static ObjectConverters.IsNotNull}}">
                            <StackPanel Spacing="4">

                                <!-- Section: Properties -->
                                <TextBlock Text="Properties" Classes="SectionHeader" />
                                <Border Classes="SectionCard">
                                    <StackPanel Spacing="10" DataContext="{Binding SelectedCompliancePolicy}">
                                        <StackPanel Spacing="2">
                                            <TextBlock Text="Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                            <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                        </StackPanel>
                                        <StackPanel Spacing="2">
                                            <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                            <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                        </StackPanel>
                                        <StackPanel Spacing="2">
                                            <TextBlock Text="Platform / Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                            <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemTypeName}"
                                                                 TextWrapping="Wrap" />
                                        </StackPanel>
                                        <StackPanel Spacing="2">
                                            <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                            <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                        </StackPanel>
                                        <Grid ColumnDefinitions="*,*">
                                            <StackPanel Grid.Column="0" Spacing="2">
                                                <TextBlock Text="Created" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding CreatedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Spacing="2">
                                                <TextBlock Text="Last Modified" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding LastModifiedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                        </Grid>
                                        <StackPanel Spacing="2">
                                            <TextBlock Text="Version" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                            <SelectableTextBlock Text="{Binding Version}" FontSize="{StaticResource FontSizeBody}" />
                                        </StackPanel>
                                        <StackPanel Spacing="2"
                                                    IsVisible="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemRoleScopeTags.Count}">
                                            <TextBlock Text="Role Scope Tags" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                            <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemRoleScopeTags, Converter={x:Static conv:StringListConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                        </StackPanel>
                                    </StackPanel>
                                </Border>

                                <!-- Section: SETTINGS -->
                                <TextBlock Text="SETTINGS" Classes="SectionHeader" />
                                <Border Classes="SectionCard">
                                    <StackPanel>
                                        <TextBlock Text="No settings"
                                                   Foreground="{DynamicResource AppTextSecondaryBrush}" FontSize="{StaticResource FontSizeBody}"
                                                   IsVisible="{Binding !SelectedItemComplianceSettings.Count}" />
                                        <ItemsControl ItemsSource="{Binding SelectedItemComplianceSettings}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate x:DataType="models:SettingItem">
                                                    <Border BorderBrush="{DynamicResource SystemControlBackgroundBaseLowBrush}"
                                                            BorderThickness="0,0,0,1" Padding="4,6">
                                                        <Grid ColumnDefinitions="*,Auto">
                                                            <TextBlock Grid.Column="0" Text="{Binding Label}" FontSize="{StaticResource FontSizeBody}"
                                                                       Foreground="{DynamicResource AppTextSecondaryBrush}"
                                                                       TextWrapping="Wrap" VerticalAlignment="Center" />
                                                            <SelectableTextBlock Grid.Column="1" Text="{Binding Value}" FontSize="{StaticResource FontSizeBody}"
                                                                                 FontWeight="SemiBold" HorizontalAlignment="Right"
                                                                                 VerticalAlignment="Center"
                                                                                 Margin="16,0,0,0" />
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>

                                <!-- Section: ACTIONS -->
                                <TextBlock Text="ACTIONS" Classes="SectionHeader" />
                                <Border Classes="SectionCard">
                                    <StackPanel>
                                        <TextBlock Text="No actions configured"
                                                   Foreground="{DynamicResource AppTextSecondaryBrush}" FontSize="{StaticResource FontSizeBody}"
                                                   IsVisible="{Binding !SelectedItemNonComplianceActions.Count}" />
                                        <ItemsControl ItemsSource="{Binding SelectedItemNonComplianceActions}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate x:DataType="models:NonComplianceActionItem">
                                                    <Border BorderBrush="{DynamicResource SystemControlBackgroundBaseLowBrush}"
                                                            BorderThickness="0,0,0,1" Padding="4,6">
                                                        <StackPanel Spacing="4">
                                                            <Grid ColumnDefinitions="*,Auto">
                                                                <TextBlock Grid.Column="0" Text="{Binding ActionTypeDisplay}"
                                                                           FontSize="{StaticResource FontSizeBody}" FontWeight="SemiBold" />
                                                                <TextBlock Grid.Column="1" Text="{Binding GracePeriodDisplay}"
                                                                           FontSize="{StaticResource FontSizeBody}" Foreground="{DynamicResource AppTextSecondaryBrush}"
                                                                           HorizontalAlignment="Right" />
                                                            </Grid>
                                                            <TextBlock Text="Notification template configured"
                                                                       FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}"
                                                                       IsVisible="{Binding HasNotificationTemplate}" />
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>

                                <!-- Section: Assignments -->
                                <controls:AssignmentListControl Assignments="{Binding SelectedItemAssignments}" />

                            </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Settings Catalog details                   -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedSettingsCatalogPolicy, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">

                                    <!-- Section: Properties -->
                                    <TextBlock Text="Properties" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedSettingsCatalogPolicy}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Name}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding OdataType}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Platforms" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Platforms}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Technologies" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Technologies}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Is Assigned" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding IsAssigned}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Role Scope Tags" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding RoleScopeTagIds, Converter={x:Static conv:StringListConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Created" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding CreatedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Last Modified" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding LastModifiedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Catalog Details -->
                                    <TextBlock Text="Catalog Details" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Settings Count" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding SelectedItemSettingsCount}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Template Families" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <ItemsControl ItemsSource="{Binding SelectedItemTemplateFamilies}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="{DynamicResource SystemAccentColor}" CornerRadius="{StaticResource RadiusSmall}"
                                                                    Padding="6,2" Margin="0,0,4,4">
                                                                <SelectableTextBlock Text="{Binding}" FontSize="{StaticResource FontSizeCaption}" Foreground="White" />
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel />
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                </ItemsControl>
                                                <TextBlock Text="No families"
                                                           Foreground="{DynamicResource AppTextSecondaryBrush}" FontSize="{StaticResource FontSizeBody}"
                                                           IsVisible="{Binding !SelectedItemTemplateFamilies.Count}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Assignments -->
                                    <controls:AssignmentListControl Assignments="{Binding SelectedItemAssignments}" />

                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Conditional Access details                 -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedConditionalAccessPolicy, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">

                                    <TextBlock Text="Policy Information" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedConditionalAccessPolicy}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="State" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding State}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Created" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding CreatedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Last Modified" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding ModifiedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="4">
                                                <TextBlock Text="Configured Controls" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <WrapPanel Orientation="Horizontal" ItemHeight="22">
                                                    <Border Background="{DynamicResource SystemAccentColor}" CornerRadius="10"
                                                            Padding="8,2" Margin="0,0,6,4"
                                                            IsVisible="{Binding Conditions, Converter={x:Static ObjectConverters.IsNotNull}}">
                                                        <SelectableTextBlock Text="Conditions" FontSize="{StaticResource FontSizeCaption}" Foreground="White" />
                                                    </Border>
                                                    <Border Background="{DynamicResource SystemAccentColor}" CornerRadius="10"
                                                            Padding="8,2" Margin="0,0,6,4"
                                                            IsVisible="{Binding GrantControls, Converter={x:Static ObjectConverters.IsNotNull}}">
                                                        <SelectableTextBlock Text="Grant Controls" FontSize="{StaticResource FontSizeCaption}" Foreground="White" />
                                                    </Border>
                                                    <Border Background="{DynamicResource SystemAccentColor}" CornerRadius="10"
                                                            Padding="8,2" Margin="0,0,6,4"
                                                            IsVisible="{Binding SessionControls, Converter={x:Static ObjectConverters.IsNotNull}}">
                                                        <SelectableTextBlock Text="Session Controls" FontSize="{StaticResource FontSizeCaption}" Foreground="White" />
                                                    </Border>
                                                </WrapPanel>
                                            </StackPanel>
                                            <StackPanel Spacing="2"
                                                        IsVisible="{Binding SessionControls, Converter={x:Static ObjectConverters.IsNotNull}}">
                                                <TextBlock Text="Session Controls" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding SessionControls}" TextWrapping="Wrap" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Conditions -->
                                    <TextBlock Text="Conditions" Classes="SectionHeaderSpaced" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <StackPanel Spacing="2"
                                                        IsVisible="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedCAPolicyIncludeLocations.Count}">
                                                <TextBlock Text="Include Locations" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedCAPolicyIncludeLocations, Converter={x:Static conv:StringListConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2"
                                                        IsVisible="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedCAPolicyExcludeLocations.Count}">
                                                <TextBlock Text="Exclude Locations" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedCAPolicyExcludeLocations, Converter={x:Static conv:StringListConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2"
                                                        IsVisible="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedCAPolicyIncludeGroups.Count}">
                                                <TextBlock Text="Include Groups" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedCAPolicyIncludeGroups, Converter={x:Static conv:StringListConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2"
                                                        IsVisible="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedCAPolicyExcludeGroups.Count}">
                                                <TextBlock Text="Exclude Groups" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedCAPolicyExcludeGroups, Converter={x:Static conv:StringListConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2"
                                                        IsVisible="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedCAPolicyIncludeApps.Count}">
                                                <TextBlock Text="Include Apps" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedCAPolicyIncludeApps, Converter={x:Static conv:StringListConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2"
                                                        IsVisible="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedCAPolicyExcludeApps.Count}">
                                                <TextBlock Text="Exclude Apps" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedCAPolicyExcludeApps, Converter={x:Static conv:StringListConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Assignment Filter details                  -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedAssignmentFilter, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">

                                    <TextBlock Text="Filter Information" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedAssignmentFilter}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Platform" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Platform}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding AssignmentFilterManagementType}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Rule" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Rule}" TextWrapping="Wrap"
                                                                     FontFamily="Consolas,Courier New,monospace" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Policy Set details                         -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedPolicySet, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">

                                    <TextBlock Text="Policy Set Information" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedPolicySet}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Items" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Items.Count}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Created" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding CreatedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Last Modified" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding LastModifiedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Application details                        -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedApplication, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">

                                    <!-- Section: Properties -->
                                    <TextBlock Text="App Information" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedApplication}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding OdataType}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="App Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemTypeName}"
                                                                         TextWrapping="Wrap" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Platform" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemPlatform}"
                                                                         FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Publisher" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Publisher}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Developer" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Developer}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Owner" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Owner}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Featured" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding IsFeatured}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Notes" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Notes}" TextWrapping="Wrap" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Information URL" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding InformationUrl}" TextWrapping="Wrap" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Privacy URL" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding PrivacyInformationUrl}" TextWrapping="Wrap" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Created" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding CreatedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Last Modified" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding LastModifiedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Publishing State" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding PublishingState}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Application Details -->
                                    <TextBlock Text="Application Details" Classes="SectionHeader"
                                               IsVisible="{Binding SelectedItemVersion, Converter={x:Static StringConverters.IsNotNullOrEmpty}, FallbackValue=false, TargetNullValue=false}" />
                                    <Border Classes="SectionCard"
                                            IsVisible="{Binding SelectedItemVersion, Converter={x:Static StringConverters.IsNotNullOrEmpty}, FallbackValue=false, TargetNullValue=false}">
                                        <StackPanel Spacing="10">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Version" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding SelectedItemVersion}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Bundle ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding SelectedItemBundleId}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Minimum OS" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding SelectedItemMinimumOS}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Install Command" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding SelectedItemInstallCommand}" TextWrapping="Wrap" FontSize="{StaticResource FontSizeBody}" FontFamily="Consolas,Courier New,monospace" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Uninstall Command" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding SelectedItemUninstallCommand}" TextWrapping="Wrap" FontSize="{StaticResource FontSizeBody}" FontFamily="Consolas,Courier New,monospace" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Install Context" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding SelectedItemInstallContext}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Size (MB)" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding SelectedItemSizeMB}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Superseded" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding SelectedItemSupersededCount}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Categories" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <ItemsControl ItemsSource="{Binding SelectedItemCategories}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="{DynamicResource SystemAccentColor}" CornerRadius="{StaticResource RadiusSmall}"
                                                                    Padding="6,2" Margin="0,0,4,4">
                                                                <SelectableTextBlock Text="{Binding}" FontSize="{StaticResource FontSizeCaption}" Foreground="White" />
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel />
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                </ItemsControl>
                                                <TextBlock Text="No categories"
                                                           Foreground="{DynamicResource AppTextSecondaryBrush}" FontSize="{StaticResource FontSizeBody}"
                                                           IsVisible="{Binding !SelectedItemCategories.Count}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Assignments -->
                                    <controls:AssignmentListControl Assignments="{Binding SelectedItemAssignments}" ShowIntent="True" />

                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Endpoint Security details                  -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedEndpointSecurityIntent, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="Policy Information" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedEndpointSecurityIntent}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Is Assigned" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding IsAssigned}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Last Modified" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding LastModifiedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                            <StackPanel Spacing="2"
                                                        IsVisible="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemRoleScopeTags.Count}">
                                                <TextBlock Text="Role Scope Tags" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemRoleScopeTags, Converter={x:Static conv:StringListConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Security Details -->
                                    <TextBlock Text="Security Details" Classes="SectionHeader"
                                               IsVisible="{Binding SelectedItemTemplateDisplayName, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                                    <Border Classes="SectionCard"
                                            IsVisible="{Binding SelectedItemTemplateDisplayName, Converter={x:Static StringConverters.IsNotNullOrEmpty}}">
                                        <StackPanel Spacing="10">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Template Display Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding SelectedItemTemplateDisplayName}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Assignments -->
                                    <controls:AssignmentListControl Assignments="{Binding SelectedItemAssignments}" />
                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Administrative Template details            -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedAdministrativeTemplate, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="Template Information" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedAdministrativeTemplate}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Ingestion Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemIngestionType}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Last Modified" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding LastModifiedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Created" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemCreatedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Assignments -->
                                    <controls:AssignmentListControl Assignments="{Binding SelectedItemAssignments}" />
                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Enrollment Configuration details           -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedEnrollmentConfiguration, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="Enrollment Configuration" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedEnrollmentConfiguration}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemDescription}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemTypeName}" TextWrapping="Wrap" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Priority" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Priority}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Created" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding CreatedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Last Modified" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding LastModifiedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <TextBlock Text="Metadata" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedEnrollmentConfiguration}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="OData Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding OdataType}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Assignments -->
                                    <controls:AssignmentListControl Assignments="{Binding SelectedItemAssignments}" />
                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- App Protection Policy details              -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedAppProtectionPolicy, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="App Protection Policy" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedAppProtectionPolicy}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemTypeName}" TextWrapping="Wrap" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Version" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Version}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Last Modified" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding LastModifiedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <TextBlock Text="Metadata" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedAppProtectionPolicy}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="OData Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding OdataType}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Version Requirements -->
                                    <TextBlock Text="Version Requirements" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <Grid ColumnDefinitions="*,*">
                                            <StackPanel Grid.Column="0" Spacing="2">
                                                <TextBlock Text="Min App Version" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemMinAppVersion}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Spacing="2">
                                                <TextBlock Text="Min OS Version" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemMinOSVersion}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                        </Grid>
                                    </Border>

                                    <!-- Section: Assignments -->
                                    <controls:AssignmentListControl Assignments="{Binding SelectedItemAssignments}" />
                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Managed Device App Configuration details   -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedManagedDeviceAppConfiguration, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="Managed Device App Configuration" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedManagedDeviceAppConfiguration}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemTypeName}" TextWrapping="Wrap" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Version" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Version}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Last Modified" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding LastModifiedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <TextBlock Text="Metadata" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedManagedDeviceAppConfiguration}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="OData Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding OdataType}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Assignments -->
                                    <controls:AssignmentListControl Assignments="{Binding SelectedItemAssignments}" />
                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Targeted Managed App Configuration details -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedTargetedManagedAppConfiguration, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="Targeted Managed App Configuration" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedTargetedManagedAppConfiguration}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemTypeName}" TextWrapping="Wrap" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Version" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Version}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Last Modified" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding LastModifiedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <TextBlock Text="Metadata" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedTargetedManagedAppConfiguration}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="OData Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding OdataType}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Assignments -->
                                    <controls:AssignmentListControl Assignments="{Binding SelectedItemAssignments}" />
                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Terms and Conditions details               -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedTermsAndConditions, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="Terms and Conditions" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedTermsAndConditions}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Version" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Version}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Created" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding CreatedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Last Modified" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding LastModifiedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <TextBlock Text="Metadata" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedTermsAndConditions}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="OData Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding OdataType}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Assignments -->
                                    <controls:AssignmentListControl Assignments="{Binding SelectedItemAssignments}" />
                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Scope Tag details                          -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedScopeTag, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="Scope Tag" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedScopeTag}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Built In" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding IsBuiltIn}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <TextBlock Text="Metadata" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedScopeTag}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="OData Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding OdataType}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Role Definition details                    -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedRoleDefinition, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="Role Definition" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedRoleDefinition}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Built In" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding IsBuiltIn}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Built In Role Definition" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding IsBuiltInRoleDefinition}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <TextBlock Text="Metadata" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedRoleDefinition}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="OData Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding OdataType}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Intune Branding details                    -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedIntuneBrandingProfile, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="Intune Branding Profile" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedIntuneBrandingProfile}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Display Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Profile Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding ProfileName}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Show Logo" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding ShowLogo}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <TextBlock Text="Metadata" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedIntuneBrandingProfile}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="OData Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding OdataType}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Azure Branding details                     -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedAzureBrandingLocalization, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="Azure Branding Localization" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedAzureBrandingLocalization}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Localization ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Sign-in Text" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding SignInPageText}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Username Hint" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding UsernameHintText}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Banner Logo URL" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding BannerLogoRelativeUrl}" TextWrapping="Wrap" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <TextBlock Text="Metadata" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedAzureBrandingLocalization}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="OData Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding OdataType}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Autopilot Profile details                  -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedAutopilotProfile, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4" DataContext="{Binding SelectedAutopilotProfile}">
                                    <TextBlock Text="Autopilot Profile" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Display Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Profile Details -->
                                    <TextBlock Text="Profile Details" Classes="SectionHeaderSpaced" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Profile Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemProfileType}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Language" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemLanguage}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Device Name Template" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemDeviceNameTemplate}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <StackPanel Spacing="2"
                                                        IsVisible="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemOobeSkipFlags.Count}">
                                                <TextBlock Text="OOBE Skip Flags" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <ItemsControl ItemsSource="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemOobeSkipFlags}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="{DynamicResource SystemAccentColor}" CornerRadius="10" Padding="8,2" Margin="0,0,4,4">
                                                                <SelectableTextBlock Text="{Binding}" FontSize="{StaticResource FontSizeCaption}" Foreground="White" />
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel />
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                </ItemsControl>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Assignments -->
                                    <controls:AssignmentListControl Assignments="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemAssignments}" />
                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Device Health Script details               -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedDeviceHealthScript, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="Device Health Script" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedDeviceHealthScript}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Display Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Script Details -->
                                    <TextBlock Text="Script Details" Classes="SectionHeaderSpaced" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedDeviceHealthScript}">
                                            <Grid ColumnDefinitions="*,*" RowDefinitions="Auto">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Publisher" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemPublisher}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Created Date" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding CreatedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <Grid ColumnDefinitions="*,*" RowDefinitions="Auto">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Run As Account" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemRunAsAccount}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Run As 32-bit" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemRunAs32BitText}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <Grid ColumnDefinitions="*,*" RowDefinitions="Auto">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Version" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemVersion}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Enforce Signature Check" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemEnforceSignatureCheck}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Detection Script -->
                                    <Expander Header="Detection Script" IsExpanded="False">
                                        <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemDetectionScript}"
                                                             FontFamily="Consolas" FontSize="{StaticResource FontSizeCaption}" TextWrapping="Wrap"
                                                             Margin="4" />
                                    </Expander>

                                    <!-- Section: Remediation Script -->
                                    <Expander Header="Remediation Script" IsExpanded="False" Margin="0,4,0,0">
                                        <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemRemediationScript}"
                                                             FontFamily="Consolas" FontSize="{StaticResource FontSizeCaption}" TextWrapping="Wrap"
                                                             Margin="4" />
                                    </Expander>

                                    <!-- Section: Assignments -->
                                    <controls:AssignmentListControl Assignments="{Binding SelectedItemAssignments}" />
                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Mac Custom Attribute details               -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedMacCustomAttribute, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4" DataContext="{Binding SelectedMacCustomAttribute}">
                                    <TextBlock Text="Mac Custom Attribute" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Display Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Assignments -->
                                    <controls:AssignmentListControl Assignments="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemAssignments}" />
                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Feature Update Profile details             -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedFeatureUpdateProfile, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4" DataContext="{Binding SelectedFeatureUpdateProfile}">
                                    <TextBlock Text="Feature Update Profile" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Display Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Update Details -->
                                    <TextBlock Text="Update Details" Classes="SectionHeaderSpaced" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Feature Update Version" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemFeatureUpdateVersion}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Rollout Start" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemRolloutStartDate, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Rollout End" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemRolloutEndDate, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Install Latest on EOL" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemInstallLatestOnEOL}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Assignments -->
                                    <controls:AssignmentListControl Assignments="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemAssignments}" />
                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Named Location details                     -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedNamedLocation, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4" DataContext="{Binding SelectedNamedLocation}">
                                    <TextBlock Text="Named Location" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Display Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Location Details -->
                                    <TextBlock Text="Location Details" Classes="SectionHeaderSpaced" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <!-- IP Ranges (shown when it's an IpNamedLocation) -->
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="IP Ranges" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <ItemsControl ItemsSource="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemIpRanges}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="{DynamicResource SystemAccentColor}" CornerRadius="{StaticResource RadiusSmall}" Padding="6,2" Margin="0,0,4,4">
                                                                <SelectableTextBlock Text="{Binding}" FontSize="{StaticResource FontSizeCaption}" Foreground="White" />
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel />
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                </ItemsControl>
                                                <TextBlock Text="Not configured" FontSize="{StaticResource FontSizeCaption}" FontStyle="Italic" Opacity="0.6"
                                                           IsVisible="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemIpRanges.Count, Converter={x:Static conv:CountToVisibilityConverter.ZeroIsVisible}}" />
                                            </StackPanel>
                                            <!-- Country Codes (shown when it's a CountryNamedLocation) -->
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Countries and Regions" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <ItemsControl ItemsSource="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemCountryCodes}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="{DynamicResource SystemAccentColor}" CornerRadius="{StaticResource RadiusSmall}" Padding="6,2" Margin="0,0,4,4">
                                                                <SelectableTextBlock Text="{Binding}" FontSize="{StaticResource FontSizeCaption}" Foreground="White" />
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel />
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                </ItemsControl>
                                                <TextBlock Text="Not configured" FontSize="{StaticResource FontSizeCaption}" FontStyle="Italic" Opacity="0.6"
                                                           IsVisible="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemCountryCodes.Count, Converter={x:Static conv:CountToVisibilityConverter.ZeroIsVisible}}" />
                                            </StackPanel>
                                            <!-- Fallback OData type -->
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Location Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding OdataType, Converter={x:Static conv:ODataTypeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <StackPanel Spacing="2"
                                                        IsVisible="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemIsTrusted}">
                                                <TextBlock Text="Trust Status" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <WrapPanel>
                                                    <Border Background="{DynamicResource SystemAccentColor}" CornerRadius="10" Padding="8,2" Margin="0,0,4,4">
                                                        <SelectableTextBlock Text="Trusted" FontSize="{StaticResource FontSizeCaption}" Foreground="White" />
                                                    </Border>
                                                </WrapPanel>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Authentication Strength details            -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedAuthenticationStrengthPolicy, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4" DataContext="{Binding SelectedAuthenticationStrengthPolicy}">
                                    <TextBlock Text="Authentication Strength" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Display Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Policy Details -->
                                    <TextBlock Text="Policy Details" Classes="SectionHeaderSpaced" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Policy Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemPolicyType}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Created" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemCreatedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2"
                                                        IsVisible="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemAllowedCombinations.Count}">
                                                <TextBlock Text="Allowed Combinations" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <ItemsControl ItemsSource="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemAllowedCombinations}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="{DynamicResource SystemAccentColor}" CornerRadius="{StaticResource RadiusSmall}" Padding="6,2" Margin="0,0,4,4">
                                                                <SelectableTextBlock Text="{Binding}" FontSize="{StaticResource FontSizeCaption}" Foreground="White" />
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel />
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                </ItemsControl>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Authentication Context details             -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedAuthenticationContextClassReference, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4" DataContext="{Binding SelectedAuthenticationContextClassReference}">
                                    <TextBlock Text="Authentication Context" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Display Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Terms of Use details                       -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedTermsOfUseAgreement, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4" DataContext="{Binding SelectedTermsOfUseAgreement}">
                                    <TextBlock Text="Terms of Use" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Display Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Per Device Acceptance" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemIsPerDeviceAcceptance}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Expiration Frequency" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemExpirationFrequency}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Assignments -->
                                    <controls:AssignmentListControl Assignments="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedItemAssignments}" />
                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- application assignments Row details                  -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedAppAssignmentRow, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4" DataContext="{Binding SelectedAppAssignmentRow}">

                                    <!-- Section: App Information -->
                                    <TextBlock Text="App Information" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="App Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding AppName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="App Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding AppType}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Platform" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Platform}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Publisher" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Publisher}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Version" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Version}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" FontSize="{StaticResource FontSizeBody}"
                                                                     MaxLines="5" TextTrimming="CharacterEllipsis" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Assignment -->
                                    <TextBlock Text="Assignment" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Assignment Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding AssignmentType}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Install Intent" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding InstallIntent}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Target Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding TargetName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Target Group ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding TargetGroupId}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8"
                                                                     IsVisible="{Binding TargetGroupId, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Is Exclusion" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding IsExclusion}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Is Featured" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding IsFeatured}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Assignment Settings" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding AssignmentSettings}" TextWrapping="Wrap" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Technical -->
                                    <TextBlock Text="Technical Details" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Bundle ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding BundleId}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Package ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding PackageId}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Minimum OS Version" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding MinimumOsVersion}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Min Disk (MB)" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding MinimumFreeDiskSpaceMB}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Min RAM (MB)" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding MinimumMemoryMB}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="2" Spacing="2">
                                                    <TextBlock Text="Min CPUs" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding MinimumProcessors}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Links & Metadata -->
                                    <TextBlock Text="Links &amp; Metadata" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Information URL" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding InformationUrl}" TextWrapping="Wrap" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Privacy URL" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding PrivacyUrl}" TextWrapping="Wrap" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="App Store URL" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding AppStoreUrl}" TextWrapping="Wrap" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Created" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding CreatedDate, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Last Modified" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding LastModified, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Categories" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Categories}" TextWrapping="Wrap" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Notes" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Notes}" TextWrapping="Wrap" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Dynamic Group details                      -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedDynamicGroupRow, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4" DataContext="{Binding SelectedDynamicGroupRow}">

                                    <TextBlock Text="Group Properties" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Group Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding GroupName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Group Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding GroupType}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Processing State" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding ProcessingState}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Group ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding GroupId}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Security Enabled" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding SecurityEnabled}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Mail Enabled" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding MailEnabled}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Created Date" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding CreatedDate, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <TextBlock Text="Membership Rule" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <SelectableTextBlock Text="{Binding MembershipRule}" TextWrapping="Wrap" FontSize="{StaticResource FontSizeBody}"
                                                             FontFamily="Consolas,Courier New,monospace" />
                                    </Border>

                                    <TextBlock Text="Membership" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <Grid ColumnDefinitions="*,*,*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Total" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding TotalMembers}" FontSize="{StaticResource FontSizeSubheading}" FontWeight="SemiBold" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Users" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Users}" FontSize="{StaticResource FontSizeSubheading}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="2" Spacing="2">
                                                    <TextBlock Text="Devices" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Devices}" FontSize="{StaticResource FontSizeSubheading}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="3" Spacing="2">
                                                    <TextBlock Text="Nested" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding NestedGroups}" FontSize="{StaticResource FontSizeSubheading}" />
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Members List -->
                                    <TextBlock Text="Members" Classes="SectionHeader" />
                                    <ProgressBar IsIndeterminate="True"
                                                 IsVisible="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).IsLoadingGroupMembers}"
                                                 Height="2" Margin="0,0,0,4" />
                                    <Border Classes="SectionCard">
                                        <StackPanel>
                                            <TextBlock Text="No members"
                                                       Foreground="{DynamicResource AppTextSecondaryBrush}" FontSize="{StaticResource FontSizeBody}"
                                                       IsVisible="{Binding !$parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedGroupMembers.Count}" />
                                            <ItemsControl ItemsSource="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedGroupMembers}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Padding="4,3" Margin="0,0,0,4">
                                                            <DockPanel>
                                                                <Border DockPanel.Dock="Left"
                                                                        Background="{DynamicResource SystemAccentColor}"
                                                                        CornerRadius="2" Padding="5,1" Margin="0,2,6,0"
                                                                        VerticalAlignment="Top">
                                                                    <TextBlock Text="{Binding MemberType}"
                                                                               FontSize="{StaticResource FontSizeCaption}" Foreground="White" />
                                                                </Border>
                                                                <TextBlock DockPanel.Dock="Right"
                                                                           Text="{Binding Status}"
                                                                           FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}"
                                                                           Margin="6,0,0,0"
                                                                           VerticalAlignment="Center"
                                                                           IsVisible="{Binding Status, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                                                                <StackPanel Spacing="1">
                                                                    <SelectableTextBlock Text="{Binding DisplayName}"
                                                                                         FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                                                    <SelectableTextBlock Text="{Binding SecondaryInfo}"
                                                                                         FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" Opacity="0.8"
                                                                                         IsVisible="{Binding SecondaryInfo, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                                                                    <SelectableTextBlock Text="{Binding TertiaryInfo}"
                                                                                         FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" Opacity="0.6"
                                                                                         IsVisible="{Binding TertiaryInfo, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                                                                </StackPanel>
                                                            </DockPanel>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Border>

                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Assigned Group details                     -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedAssignedGroupRow, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4" DataContext="{Binding SelectedAssignedGroupRow}">

                                    <TextBlock Text="Group Properties" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Group Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding GroupName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Group Type" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding GroupType}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Group ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding GroupId}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Security Enabled" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding SecurityEnabled}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Mail Enabled" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding MailEnabled}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Created Date" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding CreatedDate, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <TextBlock Text="Membership" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10">
                                            <Grid ColumnDefinitions="*,*,*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Total" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding TotalMembers}" FontSize="{StaticResource FontSizeSubheading}" FontWeight="SemiBold" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Users" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Users}" FontSize="{StaticResource FontSizeSubheading}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="2" Spacing="2">
                                                    <TextBlock Text="Devices" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding Devices}" FontSize="{StaticResource FontSizeSubheading}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="3" Spacing="2">
                                                    <TextBlock Text="Nested" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding NestedGroups}" FontSize="{StaticResource FontSizeSubheading}" />
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <!-- Section: Members List -->
                                    <TextBlock Text="Members" Classes="SectionHeader" />
                                    <ProgressBar IsIndeterminate="True"
                                                 IsVisible="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).IsLoadingGroupMembers}"
                                                 Height="2" Margin="0,0,0,4" />
                                    <Border Classes="SectionCard">
                                        <StackPanel>
                                            <TextBlock Text="No members"
                                                       Foreground="{DynamicResource AppTextSecondaryBrush}" FontSize="{StaticResource FontSizeBody}"
                                                       IsVisible="{Binding !$parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedGroupMembers.Count}" />
                                            <ItemsControl ItemsSource="{Binding $parent[ScrollViewer].((vm:MainWindowViewModel)DataContext).SelectedGroupMembers}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Padding="4,3" Margin="0,0,0,4">
                                                            <DockPanel>
                                                                <Border DockPanel.Dock="Left"
                                                                        Background="{DynamicResource SystemAccentColor}"
                                                                        CornerRadius="2" Padding="5,1" Margin="0,2,6,0"
                                                                        VerticalAlignment="Top">
                                                                    <TextBlock Text="{Binding MemberType}"
                                                                               FontSize="{StaticResource FontSizeCaption}" Foreground="White" />
                                                                </Border>
                                                                <TextBlock DockPanel.Dock="Right"
                                                                           Text="{Binding Status}"
                                                                           FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}"
                                                                           Margin="6,0,0,0"
                                                                           VerticalAlignment="Center"
                                                                           IsVisible="{Binding Status, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                                                                <StackPanel Spacing="1">
                                                                    <SelectableTextBlock Text="{Binding DisplayName}"
                                                                                         FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" />
                                                                    <SelectableTextBlock Text="{Binding SecondaryInfo}"
                                                                                         FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" Opacity="0.8"
                                                                                         IsVisible="{Binding SecondaryInfo, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                                                                    <SelectableTextBlock Text="{Binding TertiaryInfo}"
                                                                                         FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" Opacity="0.6"
                                                                                         IsVisible="{Binding TertiaryInfo, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                                                                </StackPanel>
                                                            </DockPanel>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Border>

                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Device Management Script details           -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedDeviceManagementScript, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="Properties" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedDeviceManagementScript}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Display Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="File Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding FileName}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Run As Account" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding RunAsAccount}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Run As 32-Bit" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding RunAs32Bit}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Enforce Signature Check" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding EnforceSignatureCheck}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Created" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding CreatedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Last Modified" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding LastModifiedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Script Content -->
                                    <TextBlock Text="Script Content" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <SelectableTextBlock Text="{Binding SelectedDeviceManagementScript.ScriptContent, Converter={x:Static conv:BytesToUtf8Converter.Instance}}"
                                                             FontFamily="Monospace" FontSize="{StaticResource FontSizeCaption}"
                                                             TextWrapping="Wrap"
                                                             Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                    </Border>

                                    <!-- Section: Assignments -->
                                    <controls:AssignmentListControl Assignments="{Binding SelectedItemAssignments}" />

                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Device Shell Script details                -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedDeviceShellScript, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="Properties" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedDeviceShellScript}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Display Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="File Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding FileName}" FontSize="{StaticResource FontSizeBody}" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Run As Account" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding RunAsAccount}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Execution Frequency" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding ExecutionFrequency}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Retry Count" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding RetryCount}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Block Execution Notifications" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding BlockExecutionNotifications}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Created" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding CreatedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Last Modified" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding LastModifiedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Script Content -->
                                    <TextBlock Text="Script Content" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <SelectableTextBlock Text="{Binding SelectedDeviceShellScript.ScriptContent, Converter={x:Static conv:BytesToUtf8Converter.Instance}}"
                                                             FontFamily="Monospace" FontSize="{StaticResource FontSizeCaption}"
                                                             TextWrapping="Wrap"
                                                             Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                    </Border>

                                    <!-- Section: Assignments -->
                                    <controls:AssignmentListControl Assignments="{Binding SelectedItemAssignments}" />

                                </StackPanel>
                            </ScrollViewer>

                            <!-- ═══════════════════════════════════════════ -->
                            <!-- Compliance Script details                  -->
                            <!-- ═══════════════════════════════════════════ -->
                            <ScrollViewer IsVisible="{Binding SelectedComplianceScript, Converter={x:Static ObjectConverters.IsNotNull}}"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="Properties" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <StackPanel Spacing="10" DataContext="{Binding SelectedComplianceScript}">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Display Name" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="Description" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,*">
                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock Text="Created" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding CreatedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="Last Modified" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                    <SelectableTextBlock Text="{Binding LastModifiedDateTime, Converter={x:Static conv:HumanDateTimeConverter.Instance}}" FontSize="{StaticResource FontSizeBody}" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="ID" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                                <SelectableTextBlock Text="{Binding Id}" FontSize="{StaticResource FontSizeCaption}" Opacity="0.8" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Detection Script Content -->
                                    <TextBlock Text="Detection Script" Classes="SectionHeader" />
                                    <Border Classes="SectionCard">
                                        <SelectableTextBlock Text="{Binding SelectedComplianceScript.DetectionScriptContent, Converter={x:Static conv:BytesToUtf8Converter.Instance}}"
                                                             FontFamily="Monospace" FontSize="{StaticResource FontSizeCaption}"
                                                             TextWrapping="Wrap"
                                                             Foreground="{DynamicResource AppTextSecondaryBrush}" />
                                    </Border>

                                </StackPanel>
                            </ScrollViewer>
                        </DockPanel>
                    </Grid>
                </Grid>
            </DockPanel>

            <!-- Version info (always visible, overlaid in bottom-left) -->
            <TextBlock Text="{Binding VersionText}"
                       FontSize="{StaticResource FontSizeCaption}"
                       Foreground="{DynamicResource AppTextSecondaryBrush}"
                       Opacity="0.4"
                       Margin="8,0,0,4"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Bottom"
                       IsHitTestVisible="False" />

        </Panel>
    </DockPanel>
</suki:SukiWindow>
